#+TITLE: pine docs
#+AUTHOR: Bret Horne

Every document is here, flat. A document explains code that exists or states a
design that is not built yet, and says which it is at the top. Nothing tracks
progress: what is done is what the tree does.

* Start here

| doc                             | what it is                                |
|---------------------------------+-------------------------------------------|
| [[file:philosophy.org][philosophy.org]]                  | the one idea: a Lisp Machine you cannot crash and can distribute |
| [[file:architecture.org][architecture.org]]                | the daemon, the apps that attach, the wire between them |
| [[file:pine.org][pine.org]]                        | the reference manual, by concept          |
| [[file:pine-user.org][pine-user.org]]                   | the vocabulary a config writes in init.lisp |

* The code

Written against what the tree does.

| doc                             | subsystem                                 |
|---------------------------------+-------------------------------------------|
| [[file:packages.org][packages.org]]                    | a package's name is its path; the map of them |
| [[file:api.org][api.org]]                         | the words the system is built from, and what each owns |
| [[file:errors.org][errors.org]]                      | one eval path, restarts that stay where they are live, the debugger over many images |
| [[file:treesitter.org][treesitter.org]]                  | the bindings, the band, the parser actor, the walks |
| [[file:theming.org][theming.org]]                     | one deftheme, and every renderer a consumer of it |
| [[file:wm.org][wm.org]]                          | pine as the window manager, over river   |
| [[file:hacking.org][hacking.org]]                     | editing pine with pine: recompile a defun into the running daemon |
| [[file:performance.org][performance.org]]                 | what the hot paths cost and how they were measured |

* Proposed

Designs that are argued but not built. Each says so in its first lines; read
the top before believing the rest.

| doc                             | what it proposes                          |
|---------------------------------+-------------------------------------------|
| [[file:surfaces.org][surfaces.org]]                    | a buffer as projected objects, so a tool buffer is nodes rather than text |
| [[file:properties.org][properties.org]]                  | one annotation mechanism under highlights and overlays |
| [[file:project.org][project.org]]                     | the codebase a buffer belongs to           |
| [[file:intelligence.org][intelligence.org]]                | definitions, references, completion, from the tree and the image |
| [[file:vc.org][vc.org]]                          | version control as part of the session    |
| [[file:help.org][help.org]]                        | the system answering questions about itself |
| [[file:systems.org][systems.org]]                     | what to build next, and why in that order |

* Diagrams

=architecture.dot= and =substrate.dot= are the sources; the =.png= and =.svg=
beside them are generated from those.
